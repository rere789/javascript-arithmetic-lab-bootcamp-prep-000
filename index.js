function add(x,y) {
  return x + y;
}

add(1,8); 

function subtract(x,y) {
  return x - y;
}

subract(60,40);

function multiply(x,y) {
  return x * y;
}

multiply(2,3.4);

function divide(x,y) {
  return x/y;
}

divide(5.0, 2.5);

var n = 10

function inc(n) {
  number += 5
}

inc(n)
console.log(number);
